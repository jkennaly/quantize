{
  "name": "quantize-site",
  "version": "0.0.1",
  "description": "Website to host quantize game",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node .",
    "heroku-postbuild": "cd sts-client-19 && npm run set-prod && npm run build",
    "client": "cd sts-client-19 && npm run set-prod && npm run build"
  },
  "author": "jkennaly@gmail.com",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "heroku-ssl-redirect": "0.0.4"
  }
}
